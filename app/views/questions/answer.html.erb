
 <section class="ask">
   <% if @question.blank?%>
    <%= "I can't hear your"%>
   <% elsif %>
    <% @question.end_with('?') %>
    <%= @answer%>
    <% else %>
    <%= "I don't care, get dressed and go to work!" %>
   <% end %>
 </section>

 <section class="answer">
  <p><%= params[:question] %></p>
 </section>

<%= link_to "Ask another question", ask_path %>
